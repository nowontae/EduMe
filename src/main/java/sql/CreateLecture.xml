<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edume.createLecture">

	<select id="checkLidx" resultType="Integer">
		select MAX(lidx)+1 from lecture_fake
	</select>
	<insert id="createLectureInsert" parameterType="com.edume.createLecture.model.CreateLectureDTO">
	 	 
	 insert into lecture_fake(lidx, lwritedate, lopen)
	 values(#{lidx},sysdate,'N' )
	
	</insert>
	
	<update id="createLectureUpdate" parameterType="com.edume.createLecture.model.CreateLectureDTO">
	update 
		lecture_fake
	set
		midx=#{midx},
		ltitle=#{ltitle},
		lsubtitle=#{lsubtitle},
		llevel=#{llevel},
		lsummary=#{lsummary},
		lpurpose=#{lpurpose},
		lorignprice=#{lorignprice},
		ltarget=#{ltarget},
		lknowledge=#{lknowledge},
		cat1_idx=#{cat1_idx},
		cat2_idx=#{cat2_idx},
		cat3_idx=#{cat3_idx}
    where 
    	lidx=#{lidx}
    </update>
    <update id="createLectureUpdateImg" parameterType="Map">
    update 
		lecture_fake
	set
		lthumnail=#{fileName}
    where 
    	lidx=#{lidx}
    </update>
    <select id="createLectureContent" resultType="com.edume.createLecture.model.CreateLectureDTO">
    select * from lecture_fake where lidx=#{lidx}
    </select>
    
    <select id="lectureYList" resultType="com.edume.createLecture.model.CreateLectureDTO" parameterType="Map">
    select * from lecture where lopen='Y' and midx=#{midx}
    </select>
    
     <select id="lectureNList" resultType="com.edume.createLecture.model.CreateLectureDTO" parameterType="Map">
     select * from lecture where lopen='N' and midx=#{midx}
    </select>
    
     <select id="lectureFakeList" resultType="com.edume.createLecture.model.CreateLectureDTO" parameterType="Map">
     select * from lecture_fake where midx=#{midx}
    </select>
    
</mapper>